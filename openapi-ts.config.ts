// Codegen config for the typed MindLap /api/meta client.
// Input: meta-openapi.json (copied from the gateway's /api/meta/openapi.json).
// Output: src/meta/
// TODO: configure once deps are installed.
export default {}
